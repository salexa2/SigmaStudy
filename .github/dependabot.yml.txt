version: 2
updates:
  - package-ecosystem: "pip"  # Use "pip" for Python dependencies
    directory: "/"  # This should be the directory with your requirements.txt file
    schedule:
      interval: "weekly"  # Dependabot will check for updates weekly
